detect-reverse-ssh
=================================

Add detection of reverse SSH sessions to Zeek. This package will write an entry to the notice.log whenever it observes a reverse SSH setup.

Install with

```
zkg install https://github.com/fox-ds/detect-reverse-ssh
```
