[package]
script_dir = scripts
summary = Add detection of reverse SSH sessions to Zeek.
description = Add detection of reverse SSH sessions to Zeek. This package will write an entry to the
	notice.log whenever it observes a reverse SSH setup.
depends = 
	zeek >5.1.1

[template]
source = https://github.com/zeek/package-template
version = v2.0.0
zkg_version = 2.13.0-3

[template_vars]
name = detect-reverse-ssh
